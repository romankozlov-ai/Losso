# Copy to .env.local and fill in real values. Never commit .env or .env.local.
# NEXT_PUBLIC_* are exposed to the browser.

# Example (add when needed):
# NEXT_PUBLIC_API_URL=https://api.example.com
# API_SECRET=your-secret-here
# Chatbot (DeepSeek via NVIDIA): key from https://build.nvidia.com (DeepSeek). Without it, chat shows contact fallback.
# DEEPSEEK_API_KEY=nvapi-your-key
